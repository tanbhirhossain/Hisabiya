<?php

namespace Modules\PersonalAccounting\Interfaces;

/**
 * Marker contract for the module's service layer (kept minimal on purpose:
 * each concrete service declares its own domain-specific method contract).
 */
interface ServiceInterface
{
    //
}
